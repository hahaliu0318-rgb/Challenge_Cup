# Router implementation

See the repository root README for portable deployment and testing. API and workers are the collected phase-one implementation; models and runtime state are excluded. Generate local YAML with `scripts/configure.py`.
