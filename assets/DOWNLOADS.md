# 大文件下载信息

版本：`phase1-20260903-rc1`。本次发布范围为**自训练适配器 + 经核对的官方依赖链接**；基座、视觉编码器和检索模型不重复上传到本项目账号。

## 1. 本项目适配器（百度网盘，上传待完成）

- 存储平台：百度网盘。按发布方最新选择，不再使用魔搭发布适配器。
- 计划文件名：`challenge-cup-phase1-adapter_phase1-20260903-rc1.zip`。上传与分享待完成。
- 文件：`adapter_model.safetensors`、适配器配置、处理器、词表、聊天模板及说明文件。不含基座权重、数据集、训练状态或服务器配置。
- 下载链接：待上传完成后回填。
- 提取码：待建立分享后填写。
- 访问权限：待填写分享范围和有效期，并检查接收者可访问；若平台要求登录，须显著说明。
- 模型版本：`phase1-20260903-rc1`，上传前固定包哈希，下载后核对。
- 安装：完整下载到 `general_adapter/`；必须同时准备匹配的原始非AWQ通用基座。仅有适配器不能独立推理。
- 许可与使用限制：见 [ADAPTER_LICENSE.md](ADAPTER_LICENSE.md)。

主权重大小：148,712,776字节（约141.82 MiB），服务器核验SHA256：

```text
aaaff4f5215f243b647ccb633dabff533d9bde2d28eb4cb1da12dae2b49c399c
```

该值只对应主权重，不是压缩包哈希。公开适配器配置将基座路径规范化为官方仓库ID，权重本身不改动；原服务器文件保持不变。

## 2. 官方模型依赖（已核对链接和版本）

六项依赖的固定下载入口见 [OFFICIAL_DEPENDENCIES.md](OFFICIAL_DEPENDENCIES.md)，机器可读版本见 `official_dependencies_verified.json` 和 `models_manifest.json`。其中包括平台、仓库、commit、配置哈希以及官方文件大小/LFS哈希。下载指定commit的配置、词表、处理器、全部权重分片和索引；无需下载训练状态文件。

本次检查确认官方仓库可访问，并比较已采集的本地配置；**没有对全部服务器权重逐字节比对，也没有完成在干净机器重新加载的验证**。有差异或缺少本地证据的项已单列，不把它们标成完全复现通过。

这些是作者官方依赖链接，不是本项目在魔搭/百度网盘中的镜像。参赛材料若要求所有外部大文件均位于指定平台，仍需核实是否接受该引用方式；适配器分享链接本身不能代替这项确认。

## 3. 固定运行源码（仍待解决）

文件名：`phase1-runtime-sources.zip`，应解压到 `external/`。本地已保留服务器兼容版本及逐文件清单，但第三方完整源码快照的再分发许可尚未核对完毕，因此本次不公开上传。平台、链接和包哈希均待补齐。

直接替换成上游最新分支可能丢失双卡、图像策略及内存回收等兼容修改。此项补齐并完成干净部署前，不能把本仓库称为完整一键复现版本。
