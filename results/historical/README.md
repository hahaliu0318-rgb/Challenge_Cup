# 历史评测证据，不是本次发布重跑

| 结果 | 样本数 | 正确数 | Micro | Macro | 说明 |
|---|---:|---:|---:|---:|---|
| 全局路线独立XLRS-lite评测 | 3080 | 1340 | 43.5065% | 51.5436% | 13任务对；推理错误0、解析失败2 |
| 细节路线当前MME历史结果文件 | 1297 | 566 | 43.6392% | 49.4550% | 1297唯一问题；本地从已采集预测重新核算一致 |

两行使用不同数据集，不能直接比较谁更好。全局指标按13任务对宏平均；细节指标按color/count/position三类宏平均。

注意全局 `metrics.json` 中仍保留旧的 `metric_scope` / `official_full_macro_available=false` 展示字段；同目录 `verification.json`核验了全量3080条与正式Macro，不要据旧标志把结果误写为空。原指标内容保留以便审计，没有偷偷改写它。

细节原目录名含 `half`，但本次取到的当前文件是1297条。旧盘点中的634条、54.5741%是不同规模的历史结果，不能不标明清单版本就和当前结果混用。`local_verification.json`保存重算值及原始预测文件SHA256；逐样本预测保留在维护者内部采集区，公开候选仅含聚合证据。

该细节指标的平均38.2339秒、P95 54.542秒来自批评测的单题计时，不是统一API含排队/冷启动的E2E时延。全局 `generation_mean`亦只是生成阶段，不是完整API延迟。

通用路线有包内的历史网关成功输出，但本次没有采集其完整训练/评测运行清单，因此没有把先前对话中的批量成绩重新标记为本次核验结果。
